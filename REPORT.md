# 安全开发总结报告

## 项目名称
PromptCraft - AI 提示词工场 (Secure Edition)

## 项目简介
PromptCraft 是一个 AI 提示词共享平台，让用户可以分享、发现、收藏优质的 AI 提示词。在 AI 时代，写好提示词是一种核心技能，我们的平台致力于降低 AI 使用门槛。

## 安全措施总结

### 1. 数据脱敏 (Data Masking)
- **实现方式：** 使用正则表达式对用户邮箱和手机号进行脱敏处理
- **代码位置：** `src/App.vue` - `hideEmail()` 和 `hidePhone()` 函数
- **效果示例：**
  - 邮箱：`admin@promptcraft.com` → `a***@promptcraft.com`
  - 手机号：`13812345678` → `138****5678`
- **安全意义：** 防止用户隐私信息在前端页面被完整暴露

### 2. XSS 防护 (Cross-Site Scripting Prevention)
- **实现方式：** 全局使用 `{{ }}` 插值表达式展示用户内容，禁用 `v-html`
- **代码位置：** `src/App.vue` - 所有模板中的数据绑定
- **防护原理：** Vue 的插值表达式会自动将 `<` 转义为 `&lt;`，使恶意脚本失效
- **攻击演示：** 若使用 `v-html`，输入 `<img src=x onerror="alert('被黑了')">` 会触发弹窗

### 3. SQL 注入防护 (SQL Injection Prevention)
- **实现方式：** 后端使用参数化查询（`?` 占位符）替代字符串拼接
- **代码位置：** `server/server.js` - `/api/search` 接口（修复后版本）
- **漏洞演示：** 修复前输入 `' OR '1'='1` 可获取全部数据
- **修复效果：** 使用 `?` 占位符后，用户输入被当作纯文本处理

### 4. 水平越权防护 (Broken Access Control)
- **实现方式：** 删除按钮仅在 `currentUser.id === author_id` 时显示
- **代码位置：** `src/App.vue` - 详情页删除按钮的 `v-if` 判断
- **安全意义：** 防止用户删除他人的提示词
- **补充说明：** 前端拦截仅为用户体验，后端应再次校验身份

### 5. 防暴力破解 (Brute Force Protection)
- **实现方式：** 登录失败后锁定3秒，防止快速尝试密码
- **代码位置：** `src/composables/useAuth.js` - `isLock` 状态和 `setTimeout`
- **防护效果：** 黑客无法在短时间内尝试大量密码组合

### 6. 防抖优化 (Debounce)
- **实现方式：** 搜索输入延迟500ms后才发送请求
- **代码位置：** `src/App.vue` - `watch` 监听器中的 `setTimeout`
- **安全意义：** 防止恶意脚本高频请求导致服务器崩溃

### 7. 环境变量管理 (Environment Variables)
- **实现方式：** 敏感配置存储在 `.env` 文件中，不硬编码在代码里
- **代码位置：** `.env` 文件，通过 `import.meta.env` 读取
- **安全意义：** 防止密钥泄露到代码仓库

## 技术栈
- **前端：** Vue 3 + Vite + Axios
- **后端：** Express.js + SQLite3
- **安全：** 参数化查询、输入校验、权限控制

## 总结
通过本次项目开发，我们深刻理解了 Web 安全的核心原则：
- **永远不要信任用户输入** - 所有前端校验都可能被绕过
- **前端防君子，后端防小人** - 真正的安全必须在后端实现
- **最小权限原则** - 用户只能操作自己的数据
- **防御性编程** - 假设所有输入都是恶意的
